1. **Identify the UX Issue:** The application lacks focus states for keyboard navigation, making it difficult for users relying on keyboard navigation (like hitting the `Tab` key) to know which element is currently focused.
2. **Implement Focus States:** Add `:focus-visible` styles to interactive elements like `a`, `button`, `input`, `select`, and `textarea` in `static/styles.css`. This ensures a clear visual indicator (e.g., an outline or ring) when an element receives keyboard focus.
3. **Pre-commit Checks:** Run necessary checks to ensure code quality and formatting.
4. **Submit PR:** Submit the changes with a PR describing the UX improvement.
