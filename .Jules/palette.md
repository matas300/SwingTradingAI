## 2024-05-24 - Focus Visible Styles
**Learning:** Keyboard navigation accessibility can be severely impacted if default focus outlines are stripped without providing custom styles. It is critical to explicitly define `:focus-visible` states using existing design tokens to maintain a clear visual indicator for keyboard users.
**Action:** Ensure that global `:focus-visible` styles are included in the base CSS to guarantee keyboard accessibility across all interactive elements.
